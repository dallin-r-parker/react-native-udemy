import React from "react";
import { View, Text, StyleSheet } from "react-native";

const ListScreen = () => <Text>List Screen</Text>;

const styles = StyleSheet.create({});

export default ListScreen;
